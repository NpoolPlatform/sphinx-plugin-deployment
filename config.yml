- hosts: all
  user: '{{ ansible_ssh_user }}'
  become: yes
  become_user: root
  become_method: sudo
  tasks:
    - name:
      shell: 'ls'
      register: process
      ignore_errors: true
    - name: Print processes
      debug: var=process.stdout_lines
